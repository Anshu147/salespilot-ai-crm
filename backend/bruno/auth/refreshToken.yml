info:
  name: refreshToken
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:5000/api/v1/auth/refresh
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
