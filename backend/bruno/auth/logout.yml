info:
  name: logout
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/auth/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
